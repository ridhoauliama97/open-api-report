# api.reports.mutasi-reproses.health

- Method: `POST`
- URI: `/api/reports/mutasi-reproses/health`
- Name: `api.reports.mutasi-reproses.health`
- Action: `App\Http\Controllers\MutasiReprosesController@health`
- Middleware: `api, App\Http\Middleware\AuthenticateReportJwtClaims`

## Contoh cURL

```bash
curl -X POST "http://localhost:8000/api/reports/mutasi-reproses/health"
```
